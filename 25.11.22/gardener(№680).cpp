#include iostream
#include string
#include fstream
#include vector 


using namespace std;
int main()
{
    int n;
    string string = , ts, rgb, number;
    ifstream file(C:\\l\\ lgardener.txt);
    file  n;
    while (getline(file, ts)) {
        string += ts;
    }
    file.close();
    cin  n;
    vector long long a;
    a.push_back(3);
    for (int i = 1; i  n; ++i) {
        a.push_back(a[i - 1]  2);
    }
    cout  a[n - 1];
    
    ofstream out;
    out.open(C:\\l\\loutputgardener.txt);
    out  a[n - 1];
    out.close();
    return 0;
}